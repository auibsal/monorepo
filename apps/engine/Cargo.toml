[package]
name = "idf_engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
rand = "0.8"
getrandom = { version = "0.2", features = ["js"] } # Required for rand to work in WASM

# The Neural Network Inference Engine
tract-onnx = "0.21.0"
