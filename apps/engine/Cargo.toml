[package]
name = "ida_engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
rand = "0.8"
getrandom = { version = "0.2", features = ["js"] }
tract-onnx = "0.21.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
