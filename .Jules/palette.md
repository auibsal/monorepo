## 2024-07-26 - Toolbar Accessibility
**Learning:** Abbreviated toolbar buttons (like H2, Sub) need explicit `title` and `aria-label` attributes to ensure both hover discoverability and screen reader context.
**Action:** Always add descriptive titles and aria-labels when implementing text formatting toolbar controls.
