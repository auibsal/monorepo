## 2024-05-17 - Sibling Label Associations
**Learning:** React form elements require explicit `htmlFor` on the label and `id` on the input for screen readers to properly associate them, even when grouped conceptually in the DOM. Visual indicators (like `*`) are also necessary for accessibility.
**Action:** Always verify `htmlFor` and `id` pairs on new form implementations, and ensure required fields are clearly marked.
