{
  "name": "idf-platform",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build:engine": "cd apps/engine && wasm-pack build --target web",
    "test": "npm run test --workspace=apps/web && cd apps/engine && cargo test"
  }
}
